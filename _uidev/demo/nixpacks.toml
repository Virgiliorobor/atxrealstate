[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build:demo",
  "node scripts/build-website.mjs",
]

[start]
cmd = "node dist/server/index.js"
